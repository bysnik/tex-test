\documentclass[oneside]{scrbook}
\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[russian]{babel}
\usepackage[left=3cm, right=1.5cm, top=2cm, bottom=2cm]{geometry}
\usepackage{needspace}  % для проверки свободного места
\usepackage{array}      % для управления выравниванием в колонках
\usepackage{float}      % для указания точного местоположения таблицы
\usepackage{pgffor}     % для цикла \foreach

%парсер тхт файлов
\newread\myfile
\def\readline#1#2{%
  \openin\myfile=#2\relax
  \readlinehelper{#1}{1}%
  \closein\myfile
}
\def\readlinehelper#1#2{%
  \ifeof\myfile
    % Если дошли до конца — ничего не делаем
  \else
    \read\myfile to \tmp
    \ifnum#2=#1\relax
      \tmp % выводим нужную строку
    \else
      \expandafter\readlinehelper\expandafter#1\expandafter{\number\numexpr#2+1\relax}%
    \fi
  \fi
}

\renewcommand{\arraystretch}{1.3} % немного увеличить высоту строк таблицы для лучшей читаемости

\newcommand{\examticket}[2]{% #1 = номер билета, #2 = вопросы
  \par
  \needspace{16\baselineskip}
  \noindent
  \begin{minipage}{\textwidth}
    % === Центрированная часть: шапка и таблица ===
    {\centering
      \textbf{государственное бюджетное профессиональное образовательное учреждение}\\
      \textbf{«Пермский политехнический колледж имени Н.Г. Славянова»}

      \vspace{0.4\baselineskip}

      \small

      \begin{tabular}{|%
            >{\raggedright\arraybackslash}p{0.24\linewidth}|%
            >{\centering\arraybackslash}p{0.40\linewidth}|%
            >{\raggedleft\arraybackslash}p{0.24\linewidth}|%
          }
          \hline
          Рассмотрено ПЦК\newline
          «Абракадабра»\newline
          протокол №354\newline
          от 29.02.2144 г.\newline
          Председатель ПЦК\newline
          \rule{1cm}{0.4pt} / Бубликов А.Б.\newline
          Согласовано методист\newline
          01.01.1999 г.
          &
          \textbf{Экзаменационный билет №\#1}\newline
          по учебной дисциплине\newline
          \readline{2}{parameters.txt}\unskip{}\newline
          специальность \readline{3}{parameters.txt}\unskip{}\newline
          «\readline{4}{parameters.txt}\unskip{}»\newline
          группа(ы) АБ-10, АБ-10к
          &
          \hfill Утверждаю\newline
          \mbox{}\hfill Зам. директора\newline
          \rule{1cm}{0.4pt} / К.Д. Люлайка\newline
          \mbox{}\hfill  01.01.1999 г.\newline
          \rule{1cm}{0.4pt} / Г.Д. Щупликов\par % ← КЛЮЧЕВОЕ ИЗМЕНЕНИЕ
          \\
          \hline
      \end{tabular}
      \par
    } % ← закрываем группу с \centering

    % === Вопросы — выравнивание по левому краю (по умолчанию) ===
    \vspace{0.6\baselineskip}
    \normalsize
    #2 % вопросы автоматически выровнены по левому краю

    % === Подпись преподавателя — центрированная ===
    \vspace{0.5\baselineskip}
    {\centering
      Преподаватель \rule{3cm}{0.4pt}/Н.О. Труляляков\par
    }
  \end{minipage}
  \par
  \vfill
}

\begin{document}

% Билеты формируются автоматически, также билет автоматически перейдёт на новую страницу, если не влезет.

% Автобилеты из папки
\foreach \n in {1,...,32} {
  \IfFileExists{examtickets/t\n.txt}{
    \examticket{\n}{\input{examtickets/t\n.txt}}
  }{}
}

\end{document}
